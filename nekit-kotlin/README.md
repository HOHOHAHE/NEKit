# nekit-kotlin

This directory contains the Kotlin-based components for NEKit.

The Gradle environment has been initialized. You can use the Gradle wrapper (`./gradlew`) to build, test, and manage this project.
For example, to see available tasks:
`./gradlew tasks`
To build the project:
`./gradlew build`
